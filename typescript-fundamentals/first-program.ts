const testerName: string = "Ivy Njoroge";
const role: string = "QA Engineer";
const completedTests: number = 10;
const isReady: boolean = true;

console.log(`Tester: ${testerName}`);
console.log(`Role: ${role}`);
console.log(`Completed tests: ${completedTests}`);
console.log(`Ready for automation: ${isReady}`);
